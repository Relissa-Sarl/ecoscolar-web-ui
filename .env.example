NUXT_PUBLIC_API_BASE='http://localhost:8080/api'
NUXT_PUBLIC_ENABLE_JWT='false'

# Port exposé par ecoscolar-gateway-frontend (docker compose)
NGINX_PORT=3000

# E2E Playwright contre une instance distante (NAS / SSH) — URL du front Nuxt uniquement
# PLAYWRIGHT_BASE_URL='http://100.x.x.x:3000'

